- Character set handling frontend (allow UTF-8 only for now)
- Limits on input sizes
- PUT
- access control on senstive HTTP operations e.g. DELETE
- db implementations:
  - couchdb (done)
  - mysql
    - check for tags / pairs changes - only update when needed
    - error handling
    - tests
  - hbase
  - solr?
- check thread safety LRUCache?
- notion of "buckets"