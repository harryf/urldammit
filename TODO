url parsing ( query params? )
PUT
GET via URL ( a search function - pass URI, redirect to SHA-1)
access control on senstive HTTP operations e.g. DELETE
db implementations:
 - couchdb (dropping couchdb-python)
 - mysql
 - hbase
 - solr?



